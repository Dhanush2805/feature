name: Central Organization Security

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  security-events: write

jobs:

  security-agent:
    uses: Dhanush2805/Security-Agent/.github/workflows/security-agent.yml@main
